#include<stdio.h>
#include<unistd.h>
#include<sys/types.h>

int main(){
    pid_t pid;
    pid=fork();
    if(pid<0){
        perror("Fork Failed");
        return 1;
    }
    else if(pid==0){
        printf("24SJPCCSL407");
    }
    else{
        wait(NULL);
        printf("Operating system lab");
    }
    return 0;
}
